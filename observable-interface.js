var Interface = require('interface');

/**
 * @interface ObservableInterface
 * @namespace Binding
 * @property {Event} changed
 */
var ObservableInterface = new Interface();

module.exports = ObservableInterface;